# 📰 Headline Generator (Flask + NLP)

A machine learning web application that generates concise and meaningful headlines from input text.  
This project uses a trained NLP model (saved as `model.pkl` and `tokenizer.pkl`) and a Flask backend to provide headline predictions through a simple and user-friendly web interface.

---

## 🚀 Features

- 🔤 **Text preprocessing** (cleaning, tokenization, formatting)
- 🤖 **Machine learning model** trained for headline generation
- 🧠 Uses `tokenizer.pkl` and `model.pkl` for predictions
- 🌐 **Flask web server** with a simple HTML frontend (`templates/index.html`)
- ⚡ Instant headline generation from user-provided text
- ✔ Easy to deploy and lightweight
